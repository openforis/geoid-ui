apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: geoid-managed-cert
  namespace: ${GKE_NAMESPACE}
  labels:
    app: geoid-tls
    env: ${ENVIRONMENT}
spec:
  domains:
    - ${INGRESS_HOST}
