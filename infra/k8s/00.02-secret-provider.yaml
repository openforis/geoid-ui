apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: geoid-secrets-provider
  namespace: ${GKE_NAMESPACE}
spec:
  provider: gke
  parameters:
    secrets: |
      - resourceName: "${ENV_SECRET}"
        path: ".env.local"
